import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Brand Colors (SewaSetu Emerald)
  static const Color primary = Color(0xFF0D7A57);
  static const Color primaryDark = Color(0xFF08523A);
  static const Color primaryLight = Color(0xFFE6F5EF);
  static const Color primaryHover = Color(0xFF0A6347);

  // Secondary Brand Colors (Warm Amber / Gold)
  static const Color secondary = Color(0xFFD97706);
  static const Color secondaryLight = Color(0xFFFEF3C7);
  static const Color secondaryDark = Color(0xFFB45309);

  // Service Identifier Colors
  static const Color stayService = Color(0xFF2563EB); // Blue
  static const Color stayServiceBg = Color(0xFFEFF6FF);

  static const Color tripsService = Color(0xFF7C3AED); // Purple
  static const Color tripsServiceBg = Color(0xFFF5F3FF);

  static const Color shopService = Color(0xFFD97706); // Amber
  static const Color shopServiceBg = Color(0xFFFFFBEB);

  static const Color rentalService = Color(0xFF0284C7); // Sky / Cyan
  static const Color rentalServiceBg = Color(0xFFF0F9FF);

  static const Color localExpService = Color(0xFF0D9488); // Teal
  static const Color localExpServiceBg = Color(0xFFF0FDFA);

  // Status Colors
  static const Color success = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);

  static const Color warning = Color(0xFFEA580C);
  static const Color warningLight = Color(0xFFFFEDD5);

  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEE2E2);

  static const Color info = Color(0xFF0284C7);
  static const Color infoLight = Color(0xFFE0F2FE);

  // Light Theme Neutrals
  static const Color lightBackground = Color(0xFFF8FAFC);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightBorder = Color(0xFFE2E8F0);
  static const Color lightDivider = Color(0xFFEDF2F7);

  static const Color lightTextPrimary = Color(0xFF0F172A);
  static const Color lightTextSecondary = Color(0xFF475569);
  static const Color lightTextMuted = Color(0xFF94A3B8);

  // Dark Theme Neutrals
  static const Color darkBackground = Color(0xFF0F172A);
  static const Color darkSurface = Color(0xFF1E293B);
  static const Color darkBorder = Color(0xFF334155);
  static const Color darkDivider = Color(0xFF1E293B);

  static const Color darkTextPrimary = Color(0xFFF8FAFC);
  static const Color darkTextSecondary = Color(0xFFCBD5E1);
  static const Color darkTextMuted = Color(0xFF64748B);

  // Sidebar Colors
  static const Color sidebarDark = Color(0xFF111827);
  static const Color sidebarItemHover = Color(0xFF1F2937);
  static const Color sidebarActive = Color(0xFF0D7A57);
}
